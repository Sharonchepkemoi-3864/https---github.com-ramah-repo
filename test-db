app.get('/test-db', (req, res) => {
  db.query('SELECT 1 + 1 AS result', (err, results) => {
    if (err) return res.status(500).json({ message: 'DB not working' });
    res.json({ message: 'DB is working!', result: results[0].result });
  });
});

